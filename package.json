{
  "name": "stran",
  "scripts":{
    "full-install": "npm install && cd app && npm install",
    "sc-accounts": "npx hardhat accounts",
    "sc-compile": "npx hardhat compile",
    "sc-test": "npx hardhat test",
    "sc-localnet": "npx hardhat node",
    "sc-deploy": "npm run sc-compile && npx hardhat run --network localhost scripts/deploy.js",
    "app-start": "cd app && npm start",
    "app-test": "cd app && npm test"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.3",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.5.2",
    "hardhat": "^2.8.0"
  }
}
