import { StyleSheet } from 'react-native';

export default StyleSheet.create({
	// containers
	container: {
		flex: 1,
		justifyContent: 'center',
		alignItems: 'center',
		backgroundColor: '#8b4f51',
	},

	// headings
	heading: {
		color: '#fff',
		fontSize: 40,
		marginBottom: 30,
		fontFamily: 'courier'
	},

	// buttons
	button: {
		backgroundColor: '#210130',
		padding: 10,
		marginBottom: 10,
		borderRadius: 50,
		width: 300,
		height: 50,
		justifyContent: 'center',
		alignItems: 'center'
	},

	buttonText: {
		color: '#eee',
		fontSize: 20,
	},

	// inputs
	textInput: {

	},
});
